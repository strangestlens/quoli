# Words none of the source lists carry. One per line, lowercase; # starts a
# comment. Merged on top, so anything here survives swapping a source out.
#
# Keep this short. Most of what it once held is now covered by SCOWL — if a
# word looks like it belongs in a general lexicon, check before adding it.

# came up in real play
risc

# absent from both ENABLE and SCOWL
app
wifi
zen
